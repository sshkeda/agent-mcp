{
  "name": "agent-mcp",
  "version": "0.1.0",
  "description": "Production local MCP server for agent-grade computer and repo access",
  "author": "sshkeda",
  "license": "MIT",
  "type": "module",
  "bin": {
    "agent-mcp": "dist/src/cli.js",
    "my-mcp": "dist/src/cli.js",
    "pi-mcp": "dist/src/cli.js"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json && chmod +x dist/src/cli.js",
    "dev": "tsx src/index.ts",
    "start": "node dist/src/index.js",
    "smoke": "npm run build && node tests/smoke.mjs",
    "check": "npm run build && npm run smoke"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
